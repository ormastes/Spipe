name: build

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Build and verify
        shell: bash
        run: sh scripts/build.sh
      - name: Verify native PowerShell containment
        if: runner.os == 'Windows'
        shell: pwsh
        run: ./test/windows/setup_spipe_links_containment.ps1
      - name: Verify external common PowerShell setup
        if: runner.os == 'Windows'
        shell: pwsh
        run: ./test/windows/setup_local_knowledge_external_common.ps1
      - name: Verify SPipe common discovery
        if: runner.os == 'Windows'
        shell: pwsh
        run: ./test/windows/find_spipe_discovery.ps1
