description = "Locate SPipe common and bootstrap scope-aware wiki research."
prompt = """
Run `node scripts/find-spipe.mjs --agent-guide` from an identified SPipe
checkout, or use that script from the installed common package. Respect
`SPIPE_HOME`; discovery otherwise covers project `.spipe/common`, `~/spipe`,
`~/.spipe/common`, direct checkouts, and legacy project mounts. Read every
existing path printed, starting with
`plugin/skills/spipe-research/SKILL.md`, and follow its authorization, bounded
wiki traversal, evidence, and owner-correct writeback rules. Discovery does not
grant private-scope access.
"""
